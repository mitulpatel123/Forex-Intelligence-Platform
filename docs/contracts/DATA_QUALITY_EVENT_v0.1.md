# DATA_QUALITY_EVENT v0.1

Each quality record references the raw/normalized event, stable rule ID, severity,
classification, observed value, expected condition, deterministic action, details,
UTC timestamp, and trace ID. Rule IDs are API: rename only with a schema change.

