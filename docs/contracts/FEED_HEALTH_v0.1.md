# FEED_HEALTH v0.1

Feed health summarizes EUR/USD rate/age, invalid/duplicate/out-of-order ratios,
p50/p95/p99 latency, queue/storage lag, and `HEALTHY`, `DEGRADED`, `STALE`, or
`BLOCKED` status. It is monitoring output and never blocks quote persistence.

